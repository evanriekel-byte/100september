{
  "name": "hundred-mile-september",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 execute DB --local --file=schema.sql",
    "db:remote": "wrangler d1 execute DB --remote --file=schema.sql",
    "db:migrate": "wrangler d1 execute DB --remote --file=migrate.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
